package com.mypack.todo.enums;

public enum SearchOperation {
    EQUALS,
    LIKE,
    CONTAINS,
    DATE,
    GREATER_THAN,
    LESS_THAN,
    GREATER_THAN_EQUALS,
    LESS_THAN_EQUALS,
    NOT_EQUALS,
    NOT_IN,
    IN
}
